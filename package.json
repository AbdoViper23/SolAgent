{
  "name": "solana-ai-trading-agent",
  "private": true,
  "version": "0.1.0",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm -r --parallel dev",
    "test": "anchor test",
    "lint": "pnpm -r lint"
  },
  "devDependencies": {
    "typescript": "^5.4.5",
    "@types/node": "^22.0.0",
    "shx": "^0.3.4"
  },
  "packageManager": "pnpm@9.0.0"
}
